<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <!-- Prevent PHP/script execution inside the uploads directory -->
    <handlers>
      <clear />
    </handlers>
  </system.webServer>
</configuration>
